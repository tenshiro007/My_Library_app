<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.p003_mylibraly">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.P003_myLibraly">
        <activity android:name=".FavoriteBookActivity" android:label="Favorite Books"></activity>
        <activity android:name=".WishBookActivity" android:label="Wishlist Books"/>
        <activity android:name=".AlreadyReadBookActivity" android:label="Already Read Books" />
        <activity android:name=".CurrentlyReadActivity" android:label="Currently Read Books"/>
        <activity android:name=".BookActivity" android:label="Book Details"/>
        <activity android:name=".AllBookActivity" android:label="Books"/>
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>